<?php

defined('MOODLE_INTERNAL') || die();

function xmldb_local_aicc_hacp_install() {
    // This function is intentionally empty. The database tables are defined in install.xml.
    return true;
}
